

#

sudo apt-get install ruby-dev libsqlite3-dev ; sudo gem install sqlite3
sudo gem install clamp
sudo gem install colorize
sudo apt-get install nmap ; sudo gem install netaddr
sudo gem install net-ping


